import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { SessionProvider } from "next-auth/react";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Maternal Health Companion Platform - Affordable Healthcare for Every Woman",
  description: "Empowering mothers with accessible, affordable, and stress-free healthcare from Ogbomoso and beyond. Digital medical history, consultations, and comprehensive maternal health services.",
  keywords: "maternal health, healthcare, women's health, pregnancy, nursing mothers, Ogbomoso, digital health, medical records",
  authors: [{ name: "Maternal Health Companion Platform" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <SessionProvider>
          {children}
        </SessionProvider>
      </body>
    </html>
  );
}
